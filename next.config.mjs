/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['raw.githubusercontent.com', 'avatars.githubusercontent.com', 'eng.kku.ac.th'],
    }
};

export default nextConfig;
